# Contract

The contract stores remittance records with owner, target amount, released amount, status, and score.

Main method:

`recordRemittanceRecord(id, actor, amount, score)`

Use this method after backend validation and wallet approval.
